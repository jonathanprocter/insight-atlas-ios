.DS_Store
._*
*.xcresult
*.xcuserstate
*.xccheckout
*.xcuserdatad
DerivedData/
build/
node_modules/
dist/
archive_output/
extracted_project/

# Temporary files
.tmp/
*.lock

# Credentials — never commit
AuthKey_*.p8
*.p8
*.mobileprovision
*.cer
*.certSigningRequest
.env
.env.*
ExportOptions.plist
secrets.json
*.secret

# Design mockups, archives and vendored templates — kept locally, not in the
# public repository. None are build inputs; the Xcode project references none.
*.zip
mockup_*.png
mockup_*.PNG
mockup_*.JPG
InsightAtlas-Master-Template-FINAL.html
full-font-embed.css
*.ttf.base64
